from opcua import uamethod
from .tools import argget
import time
import threading  # Used for running methods on threads, so it doesn't block the app

import logging  # May be used to know when a thread is running, may be deleted from this one

# This file is useless. May be nice if it works as I wish, but I need some globals and I cannot use them here.


